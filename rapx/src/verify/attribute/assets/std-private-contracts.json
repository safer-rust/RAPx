{
  "SliceExt::get_unchecked_ext": [
    {
      "tag": "InBound",
      "args": ["index_access(self, index)"]
    }
  ],
  "SliceExt::get_unchecked_mut_ext": [
    {
      "tag": "InBound",
      "args": ["index_access(self, index)"]
    }
  ],
  "SliceExt::split_at_unchecked_ext": [
    {
      "tag": "ValidNum",
      "args": ["mid <= len(self)"]
    }
  ],
  "SliceExt::split_at_mut_unchecked_ext": [
    {
      "tag": "ValidNum",
      "args": ["mid <= len(self)"]
    }
  ],
  "SliceExt::swap_unchecked_ext": [
    {
      "tag": "ValidNum",
      "args": ["a < len(self)"]
    },
    {
      "tag": "ValidNum",
      "args": ["b < len(self)"]
    }
  ],
  "SliceExt::as_chunks_unchecked_ext": [
    {
      "tag": "ValidNum",
      "args": ["N >= 1"]
    },
    {
      "tag": "ValidNum",
      "args": ["len(self) % N == 0"]
    }
  ],
  "SliceExt::as_chunks_unchecked_mut_ext": [
    {
      "tag": "ValidNum",
      "args": ["N >= 1"]
    },
    {
      "tag": "ValidNum",
      "args": ["len(self) % N == 0"]
    }
  ],
  "SliceExt::align_to_ext": [
    {
      "tag": "Align",
      "args": ["self", "T"]
    },
    {
      "tag": "Typed",
      "args": ["self", "U"]
    }
  ],
  "SliceExt::align_to_mut_ext": [
    {
      "tag": "Align",
      "args": ["self", "T"]
    },
    {
      "tag": "Typed",
      "args": ["self", "U"]
    }
  ],
  "SliceExt::get_disjoint_unchecked_mut_ext": [
    {
      "tag": "InBound",
      "args": ["index_access(self, indices)"]
    },
    {
      "tag": "NonOverlap",
      "args": ["indices"]
    }
  ]
}
